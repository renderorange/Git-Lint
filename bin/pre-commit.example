#!/bin/bash

# Update the path and options to match your path and preferred profile.

# To enable as a pre-commit hook, create a symlink to this script named
# pre-commit in the .git/hooks directory of the repo you want to check.
# /repo/.git/hooks (GIT_DIR!) $ ln -s ~/git/Git-Lint/bin/pre-commit.example pre-commit

perl ~/git/Git-Lint/bin/git-lint --profile default
